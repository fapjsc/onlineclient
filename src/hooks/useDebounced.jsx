// import { useCallback } from 'react';
// // Lodash
// import debounce from 'lodash.debounce';

// const useDebounced = (fn, timer) => {
//   const debounced = useCallback(() => debounce(fn, timer), [fn, timer]);
//   return {
//     debounced,
//   };
// };

// export default useDebounced;
